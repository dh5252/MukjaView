package capstone.mukjaView.Repository;

import capstone.mukjaView.Domain.RestaurantDetailedPicture;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RestaurantDetailedPictureRepository extends JpaRepository<RestaurantDetailedPicture, String> {
}
